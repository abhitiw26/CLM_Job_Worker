import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

//@SpringBootTest(classes = CLMJobWorkerStarterApplication.class)
public class AuthenticationTest {
	
	Logger LOG = LoggerFactory.getLogger(AuthenticationTest.class);
	
	/*
	 * @Test void testAuthentication() { String token =
	 * AuthenticationUtil.getAuthToken(); LOG.info("testAuthentication:: Token->",
	 * token); }
	 */
}
